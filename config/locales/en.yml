en:
  session:
    error:
      validation: 'invalid email or password'
  facebook:
    session:
      error:
        validation: 'invalid access token'

  time:
    formats:
      default: '%d %b %y, %H:%M'

  :true: 'True'
  :false: 'False'

